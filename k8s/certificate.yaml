apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: bankapp-cert
  namespace: bankapp
spec:
  secretName: bankapp-tls  
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - 35.154.207.215.nip.io    
